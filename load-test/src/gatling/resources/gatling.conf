gatling {
  http {
    # 10s: long enough for a normal API call, short enough that a stuck user is
    # marked KO instead of sitting in the run and skewing the percentiles.
    # Override per run with -Dgatling.http.requestTimeout=… if a simulation
    # deliberately exercises something slow.
    requestTimeout = 10000
  }
}
