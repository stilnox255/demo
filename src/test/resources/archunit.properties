# ArchUnit configuration for the layering fitness functions (ADR-01).
# failOnEmptyShould=false so a rule whose target packages do not exist yet in a
# freshly bootstrapped project passes instead of failing on an empty set. Once a
# BC is added, the rules begin checking real classes.
archRule.failOnEmptyShould=false
